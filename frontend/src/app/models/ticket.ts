export interface Ticket {
    id: number; // Adjust property name as per your backend
    customerName: string;
    ticketTitle: string;
    ticketIssue: string;
    sixDigitTicketNumber: number;
    ticketStatus: boolean;
  }

